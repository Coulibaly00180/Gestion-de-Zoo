{% extends 'base.html.twig' %}

{% block title %}Supprimer l'espace{% endblock %}

{% block body %}
    <h1>Supprimer l'espace <i>{{ espace.nom }} ?</i></h1>
    {{ form(formulaire) }}
    {#je vais mettre un petit bouton retour...#}
    <a href="{{ path("app_espace") }}" class="btn btn-default">Retour</a>
{% endblock %}